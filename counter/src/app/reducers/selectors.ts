
import {  Models }  from '@app/reducers/models';
import { createSelector } from 'reselect';

export function getSelectorCollection(){
    return {
        getCollectionLoaded:""
    }
}


function createSelec(){
    
}

//export const getCollectionLoaded = createSelector("","");
/*export const getCollectionLoading = createSelector(model, fromCollection.getLoading);
export const getCollectionBookIds = createSelector(model, fromCollection.getIds);*/
